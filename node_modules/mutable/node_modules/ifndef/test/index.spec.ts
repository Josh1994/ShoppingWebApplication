import {expect} from 'chai';
import {name as name1} from './test-example/a'
import {name as name2} from './test-example/b'

describe('Test skeleton', function () {
    it('Should export only the First module', function () {
        expect(name1).to.be.equal(name2)
        expect(name2).to.be.equal('First')
    })

});
